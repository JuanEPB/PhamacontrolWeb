# Frontend public config.
# Only variables prefixed with VITE_ are exposed to the browser.
# Do not place server secrets, SMTP passwords, database URLs, JWT secrets,
# Django secrets, CSRF settings, or cookie secrets in this frontend env file.
VITE_API_URL=http://localhost:3000
